<%= form_with(model: exam_type) do |form| %>
  <% if exam_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam_type.errors.count, "error") %> prohibited this exam_type from being saved:</h2>

      <ul>
        <% exam_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cost_on_argentinian_pesos %>
    <%= form.number_field :cost_on_argentinian_pesos %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
