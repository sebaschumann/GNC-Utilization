<%= form_with(model: tube) do |form| %>
  <% if tube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tube.errors.count, "error") %> prohibited this tube from being saved:</h2>

      <ul>
        <% tube.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :diameter_on_milimeters %>
    <%= form.number_field :diameter_on_milimeters %>
  </div>

  <div class="field">
    <%= form.label :capacity_on_liters %>
    <%= form.number_field :capacity_on_liters %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.date_select :expiration_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
