<%= form_with(model: locality) do |form| %>
  <% if locality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(locality.errors.count, "error") %> prohibited this locality from being saved:</h2>

      <ul>
        <% locality.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :province_id %>
    <%= form.collection_select(:province_id, Province.all, :id, :province_country) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
