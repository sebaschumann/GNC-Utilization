<%= form_with(model: installation_employee) do |form| %>
  <% if installation_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(installation_employee.errors.count, "error") %> prohibited this installation_employee from being saved:</h2>

      <ul>
        <% installation_employee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kit_installation_id %>
    <%= form.collection_select(:kit_installation_id, KitInstallation.all, :id, :id) %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.collection_select(:employee_id, Employee.all, :id, :full_name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
