<%= form_with(model: version) do |form| %>
  <% if version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(version.errors.count, "error") %> prohibited this version from being saved:</h2>

      <ul>
        <% version.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :version %>
    <%= form.text_field :version %>
  </div>

  <div class="field">
    <%= form.label :model_id %>
    <%= form.collection_select(:model_id, Model.all, :id, :model_brand) %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
